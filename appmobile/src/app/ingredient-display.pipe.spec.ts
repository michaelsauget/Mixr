import { IngredientDisplayPipe } from './ingredient-display.pipe';

describe('IngredientDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new IngredientDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
